# Keystore Configuration
# Copy this file to keystore.properties and fill in your actual values
# DO NOT commit keystore.properties to git!

# Path to your keystore file (relative to project root or absolute path)
storeFile=../genai-video-keystore.jks

# Keystore password
storePassword=YourKeystorePasswordHere

# Key alias (the name you gave your key when creating the keystore)
keyAlias=genai-video-key

# Key password (can be same as storePassword)
keyPassword=YourKeyPasswordHere












